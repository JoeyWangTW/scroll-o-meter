# Match the working iphone-scroll-encoder conf: ZMK_MOUSE alone gives
# the legacy 8-bit wheel HID report iOS consumes (it auto-enables
# ZMK_POINTING).
CONFIG_ZMK_MOUSE=y
# CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y

# Rotary encoder
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# Persistence for the scroll counter
CONFIG_SETTINGS=y
CONFIG_NVS=y

# Match agent-keyboard's iphone-scroll-encoder branch exactly: no
# polling kscan, no USB logging. Both encoders work there with this
# minimal conf.
